## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues (repo will be pushed to GitHub soon). Uses the `gh` CLI. PRs are not treated as a triage surface. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
